general:
  branches:
    only:
      - master
      
dependencies:
  pre:
    - sudo apt-get install cmake
test:
  pre:
    - mkdir build && cd build && cmake -D CMAKE_BUILD_TYPE=Debug -D ENABLE_TEST=ON ..
  override:
    - cd build && make && make runtests

