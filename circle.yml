general:
  branches:
    only:
      - master
      
dependencies:
  pre:
    - sudo apt-get install cmake
test:
  pre:
    - ll -lh
    - mkdir build
    - cd build
    - pwd
    - ll -lh
    - cmake -D CMAKE_BUILD_TYPE=Debug -D ENABLE_TEST=ON ..
  override:
    - make && make runtests

